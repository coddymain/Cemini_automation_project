# Итоговый план проекта: Enterprise QA Automation (Python)

## 🎯 1. Цель и Стек (Goal and Stack)
*   **Стек:** Python, Playwright, Pytest. Только современные подходы (актуальные на 2026 год).
*   **Среда разработки:** macOS, редактор VS Code.
*   **Менеджер пакетов (Package Manager):** Poetry (вместо устаревшего requirements.txt) для жесткой фиксации версий и автоматического управления окружением.
*   **Итоговая цель:** Уровень QA Automation Engineer Middle. Умение с нуля строить масштабируемую архитектуру, писать чистый код и понимать каждую строчку.

## 🏗 2. Архитектура и Подходы (Architecture and Approaches)
*   **Структура:** Enterprise (корпоративная) архитектура. Проект должен легко масштабироваться на тысячи тестов.
*   **Управление данными (Data Management):** 
    *   Файлы `.env` для хранения секретов (логины, пароли, базовые URL).
    *   `Pydantic` или `Data Classes` для строгой типизации тестовых данных (например, информации о товарах).
*   **Паттерны (Patterns):** Современная реализация Page Object Model (POM), активное использование фикстур (fixtures) Pytest для внедрения зависимостей (Dependency Injection) и управления состоянием.
*   **Логирование (Logging):** Внедрение системы логирования (например, `loguru` или встроенный модуль `logging`) для подробного отслеживания шагов автотестов и упрощения отладки в CI.

## 🎯 3. Что тестируем (Target)
*   **Приложение:** Swag Labs (https://www.saucedemo.com/).
*   **Фокус:** Комплексный подход: UI (пользовательский интерфейс) и API (интеграционное тестирование, быстрая подготовка данных перед тестами).
*   **Главный сценарий:** E2E (End-to-End). Полный путь пользователя от авторизации до успешного оформления заказа.

## 🚀 4. Выполнение и Отчетность (Execution and Reporting)
*   **Запуск (Execution):** 
    1.  Последовательный (sequentially) — для детального разбора и отладки.
    2.  Параллельный (parallel execution) через `pytest-xdist` — для скорости (настроим позже).
*   **Отчеты (Reporting):** Интеграция с Allure Framework. Обязательное автоматическое прикрепление скриншотов (screenshots) и видеозаписей (videos) прохождения тестов.
*   **Интеграция CI/CD:** Автоматический запуск автотестов в облаке через GitHub Actions. Интеграция Allure-отчетов в пайплайн и безопасная работа с GitHub Secrets.

## 📚 5. Формат обучения (Learning Format)
*   **Поэтапно:** Создаем файлы один за другим, без генерации всего проекта сразу.
*   **Шпаргалки (Cheat Sheets):** Весь процесс логируется в папке `docs/` в виде Markdown-файлов. Это ваша база знаний.
*   **Детальный разбор:** Каждая строчка кода комментируется: зачем нужна, откуда берется, куда идет.
*   **Без "воды":** Только суть, с примерами. Подсвечиваем старые подходы (как делать не надо) и современные стандарты.

## 🗺 6. Дорожная карта и Статус (Roadmap & Status)
### ✅ Выполнено (Done)
*   [x] **Фундамент:** Настройка Poetry, установка Pytest и Playwright.
*   [x] **Конфигурация:** Настройка `.env` и Pydantic `Settings` (строгая типизация).
*   [x] **Архитектура:** Построение Page Object Model (POM) и настройка BasePage.
*   [x] **Pytest Ядро:** Настройка фикстур, внедрение зависимостей (DI) в `conftest.py`.
*   [x] **Отчетность:** Интеграция Allure, скриншоты при падении через хуки, видео и трейсы.
*   [x] **CI/CD:** Настройка GitHub Actions (`playwright.yml`), GitHub Secrets и решение проблем с `package-mode`.

### 🚀 В планах (To Do)
*   [x] **Логирование:** Установка и настройка `loguru` для прозрачности шагов автотестов (info/debug).
*   [x] **Финализация E2E:** Дописывание Page Objects для полного завершения покупки (`CheckoutOverviewPage`, `CheckoutCompletePage`).
*   [x] **API Тестирование:** Интеграция Playwright API (пропуск UI-логина, гибридные подходы).
*   [x] **Масштабирование:** Настройка параллельного запуска тестов через плагин `pytest-xdist`.

## 🤖 7. Правила для AI-агентов (AI Context & Coding Rules)
**Внимание любому AI-ассистенту:** При генерации нового кода для этого проекта строго соблюдай следующие современные стандарты (2026 год):

1.  **Типизация:** Обязательное использование Type Hints для всех аргументов и возвращаемых значений (например, `def func(page: Page) -> None:`).
2.  **Playwright:** Используем только синхронное API (`playwright.sync_api`). Запрещено использовать встроенный `assert`, используем только умные ожидания `expect()`.
3.  **Архитектура POM:** 
    *   Локаторы инкапсулируются через `@property` (например, `def _login_button(self) -> Locator:`).
    *   Методы страниц не должны содержать проверок (`expect`), проверки остаются только в файлах тестов (паттерн AAA).
4.  **Данные и конфиги:** Запрещено использовать `os.getenv`. Все переменные окружения читаются только через `core.settings.py` (Pydantic v2 `BaseSettings` + `SettingsConfigDict`).
5.  **Фикстуры:** Тесты не должны сами инстанцировать объекты страниц (не писать `login_page = LoginPage(page)` в тесте). Объекты передаются исключительно через Dependency Injection из `conftest.py`.
6.  **Язык:** Документация (docstrings) и комментарии пишутся на русском языке. Код (переменные, функции) — на английском.

**Твоя задача как AI:** Если пользователь просит создать новую страницу или тест, сначала сверься с этими правилами, чтобы код сразу соответствовал Enterprise-стандартам проекта.
